---
description: Реализовать один подтверждённый этап EventFlow
agent: build
---

Реализуй только указанный ниже подтверждённый этап EventFlow.

Сначала прочитай `AGENTS.md`, `references/project-brief.md` и относящиеся к этапу критерии из `references/acceptance-checklist.md`.

Правила:

- изменяй только файлы внутри `app/`;
- не изменяй исходники в `data/` и `references/`;
- не подключай внешние зависимости;
- не заменяй IndexedDB на массив или localStorage;
- не переписывай уже работающие части без необходимости;
- при изменении схемы явно объясни миграцию;
- после работы перечисли изменённые файлы;
- дай точный список ручных проверок;
- явно сообщи, что осталось на следующий этап.

Этап для реализации: $ARGUMENTS

