---
description: Спроектировать данные и пользовательские сценарии EventFlow без изменения файлов
agent: plan
---

Работай только как архитектор. Не создавай, не изменяй и не удаляй файлы.

Изучи:

- `AGENTS.md`;
- `references/project-brief.md`;
- `references/acceptance-checklist.md`;
- `data/event-settings.json`;
- заголовки и несколько строк обоих CSV-файлов;
- доступный визуальный референс.

Подготовь план:

1. пользовательский путь от пустого приложения до заполненного дашборда;
2. модель участника и бизнес-правила;
3. схему IndexedDB, хранилища, ключи, индексы и транзакции;
4. структуру файлов внутри `app/`;
5. CRUD, поиск, фильтры и сортировку;
6. конвейер CSV: файл → предпросмотр → проверка → подтверждение → база;
7. экспорт и восстановление JSON;
8. состояния loading, empty, error и success;
9. этапы реализации и критерии проверки каждого этапа;
10. риски потери данных и способы сделать действия обратимыми.

Не предлагай фреймворки, npm, сервер, внешние библиотеки, API или авторизацию.

Дополнительный фокус пользователя: $ARGUMENTS

